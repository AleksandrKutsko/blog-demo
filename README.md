# Тестовый блог на чистом PHP с Docker-окружением

Простой блог на чистом PHP с использованием MySQL и Smarty.

## ✨ Ключевые особенности

### 🏗️ Архитектура и код
- **Чистый PHP 8.3** — без фреймворков, но с современными практиками
- **MVC-подход** — четкое разделение логики (Модели, Контроллеры, Представления)
- **PSR-4 автозагрузка** — правильная организация неймспейсов
- **ООП с паттернами** — Singleton для БД, Active Record для моделей
- **Документированный код** — методы имеют PHPDoc блоки

### 🚀 Роутинг
- Собственный роутер с поддержкой:
- REST методов (GET, POST, PUT, DELETE)
- Динамических параметров (`/post/{id}`)
- Именованных маршрутов
- Опциональных параметров

### 💾 Работа с БД
- **MySQL** с подготовленными запросами (защита от SQL-инъекций)
- **PDO** — современный и безопасный подход
- **Индексы** для оптимизации запросов
- **Сидинг** — автоматическое заполнение тестовыми данными

### 🎨 Шаблонизатор Smarty
- **Чистое разделение логики и представления**
- Собственные модификаторы и функции
- Наследование шаблонов (layout -> страницы)
- Удобная работа с данными в шаблонах

### 📱 Интерфейс
- **Адаптивная верстка**
- Пагинация с номерами страниц
- Сортировка по просмотрам/дате через GET-параметры

### 🐳 Docker-окружение
- Nginx — веб-сервер
- PHP 8.3 — интерпретатор
- MySQL 8.0 — база данных
- Adminer — удобное управление БД

## 🚀 Быстрый старт

### Требования
- Docker
- Docker Compose
- Git

### Установка

1. **Клонировать репозиторий**
   ```bash
   git clone https://github.com/AleksandrKutsko/blog-demo.git

2. **Запустить Docker контейнеры**

    ```bash
    docker-compose up -d --build

3. **Установить зависимости через Composer**

    ```bash
    docker-compose exec php composer install

4. **Заполнить базу тестовыми данными**
    ```bash
   docker-compose exec php bash
   php seed.php

5. **Открыть в браузере**
http://localhost